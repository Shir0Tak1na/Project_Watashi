# **摘要**

*Project:渡（Project Watashi）*

本项目是一款专注于解决网络流行语及科幻、魔幻等非现实虚构词汇翻译难题的智能辅助软件。致力于为网文出海、游戏本地化、字幕组及泛娱乐文化出海提供高效、低成本、高精度的翻译解决方案。

传统翻译工具及现有云端 AI（如常规 API 调用）在处理“网络梗”、“网文修仙术语”或“科幻自造词”时，往往出现语义缺失、翻译生硬，甚至无法翻译的情况，导致人工译后编辑耗时费力，争议频发。

本产品通过重构人工智能翻译逻辑，在本地算法中嵌入特定翻译原则，针对虚构词汇进行自动化加工与双语转换。软件主要用于即时屏幕识别翻译，有效辅助人工翻译工作，大幅减少云端 API 调用成本，实现更优秀的翻译速度与数据安全。

本项目为软件工程产品，包含一套基于桌面端的“即时屏幕翻译识别工具”及配套的“自定义语料库 / 规则库”模块。跨平台与移动端不再是远期设想，而是本项目的正式扩展需求（见“功能扩展”）。

---

# **基础功能**

**实时识别屏幕上指定区域的指定语言文字，翻译为设定的目标语言文字。**

即：用户圈定屏幕区域 → 指定源语言文字（或自动检测）与目标语言文字 → 系统持续实时识别该区域文字并输出译文，以**双语对照**形式呈现，无需复制粘贴。

## 要求

### R1 功能全部本地部署，不依赖云端 API

- 断网状态下，OCR、语料库匹配、规则推演、LLM 推理全链路必须可用。
- 程序主链路中不得存在强制的云端调用；任何联网行为必须为可选且默认关闭。
- 各环节的本地实现方式：

| 环节 | 本地实现 |
| --- | --- |
| 屏幕捕获 | 各平台原生截屏 API |
| OCR | 本地 OCR 模型（如 ONNX Runtime 承载的轻量中英文模型） |
| 翻译 | 本地语料库匹配 + 规则推演，必要时由**本地 LLM** 辅助 |
| LLM | llama.cpp / ONNX Runtime 等本地推理后端，加载量化模型 |
| 存储 | 本地 SQLite 与 JSON 文件（语料库、缓存、配置） |

- 云端大模型 API 仅作为**可插拔的可选插件**存在，不参与默认路径。

### R2 支持自定义语料库

- 用户可通过编写语料库文件，实现**网络用语**与**非现实存在的词汇**（科幻/魔幻自造词）的翻译。
- 语料库分级管理，支持热加载与增量扩充：

| 层级 | 内容 | 典型路径 |
| --- | --- | --- |
| 通用词典 | 基础高频词 | `rules/dictionaries/` |
| 网络用语库 | 网络热梗、游戏黑话 | `rules/slang/` |
| 虚构词汇库 | 修仙、魔幻、科幻自造词 | `rules/fiction/` |
| 用户私有库 | 用户个人术语，优先级最高 | `plugins/user/custom_rules/` |

- 词条字段至少包含：源词、译文、词性、领域标签、优先级、来源、更新时间、**目标语言**。
- **词条必须能表达"这条译文是什么语言"**。语料库不是语言中立的：一份英→中词表在被要求译成
  日语时会**满置信度地给出中文**——这比"读回原文"更难发现，因为两边都是外文，看起来没有异常。
  引擎无法推断它拿到的译文的语言，只有写词条的人知道，所以词条自己声明（文件级
  `"lang": "zh-CN"`，或每条 `lang`）；不声明视为"任何目标语言都可用"，以兼容既有文件。
  语言按语言比较而非字符串比较：`zh` / `zh-CN` / `zh_TW` / `zho_Hans` 命中同一条词条。
  同一源词可分别为不同语言各存一条，互不覆盖。
- 冲突解决策略：**用户私有库 > 领域库 > 通用词典**，同一层级内**最长匹配优先**。
- **规则的语言过滤同理**：规则的 `target` 按语言比较（`zh` 等价于 `zh-CN`），规则内部查语料库
  时也带上自己的语言，否则中文规则会拿日语词条拼出自己的答案。正则编译失败的规则直接停用并
  报告，而不是留在"已加载规则数"里冒充可用。
- **实时纠正（自学习闭环）**：译错了就地在屏幕上改。选中那一行、填上正确的译文即完成记录，
  写入用户私有库的 `corrections.json`（该层不存在时自动创建，永不改写用户手写的语料文件），
  当前这一帧立刻改过来，此后同样的句子都用它。两种粒度，差别是能不能扛住 OCR 抖动：

| 粒度 | 匹配方式 | 适用场景 |
| --- | --- | --- |
| 整句 | 整句比对，忽略首尾标点与空白 —— OCR 每次读同一句的标点都不同 | 某一句整体译错 |
| 词语 | 该词在任何句子中都替换 | 反复出现的专有名词、人名、招式名 |

- **屏幕识别必须排除自身**：引擎的工作是"拍一块屏幕→认字→翻译"，所以只要它自己的窗口落在那块屏幕里，
  就是在读自己的输出 —— 字幕条会读到上一句译文，设置窗口会让画面每刷新一个计数器就重新识别一次，
  表现为"一启动就卡"。分两类处理：

| 情况 | 处理 |
| --- | --- |
| **自己拥有的窗口**（悬浮窗、设置窗口） | 向 Windows 申请不参与截屏（`WDA_EXCLUDEFROMCAPTURE`）。悬浮窗一直如此；设置窗口现在也一样，自检断言该标志确实生效（`0x11`，并在关闭该设置时以 `0x0` 作对照） |
| **不属于自己的窗口**（浏览器里打开的设置面板） | 无法申请——那是别的进程的窗口。因此引擎改为**检测**：区域覆盖到它就拒绝开始，并说明是哪个窗口、占区域多少；手动「继续识别」不会被拦，只会提示 |

- **状态必须立刻可见**：暂停/继续要马上反映在按钮与旁边的一行状态上。此前 stats 事件只在"处理完一帧"
  之后发布，而暂停后不再处理帧，于是最后一条 stats 永远停在 `paused: False`，界面一直显示"识别中"——
  用户看到的就是"按了停止没有任何变化"。现在暂停/继续本身会发布 stats，按钮也直接用命令返回值更新。

- 纠正在引擎里**优先于一切**：语料库本身译错时，错误的译文是一次"高置信度命中"而非空缺，
  再加多少词条都没用，只有人能覆盖它。整句纠正被视为一条完整术语，模型不会再去"改进"它。
- **界面内编辑语料库（含出厂库）**：Web 面板「语料库」页就是编辑器 —— 表格化列出所有层的词条，
  支持新增/修改/覆盖/停用/还原，以及 JSON、CSV/TSV 导入导出（其它格式走插件 `corpus_loader`）。
  三条硬规则：

| 规则 | 做法 |
| --- | --- |
| **出厂文件永不被改写** | 编辑出厂词条 = 在用户层写一条同名覆盖（层级优先），界面标出「已覆盖」并显示出厂原文，可一键还原；`selfcheck_library` 逐字节断言出厂文件未变 |
| **停用 ≠ 假翻译** | 不想用出厂某条时写入 `_suppress` 列表让它整体不生效，而不是塞一条空译文；被停用的词条仍在列表里标出，可还原 |
| **改完立刻生效** | 写完文件强制重载一次（不是等 mtime 节流），并广播事件让其它界面重绘表格 |

- 界面分工（0.0.8 起）：**Web 面板 = 配置与语料编辑**（50 项设置表单、呈现规格、词条表格、
  导入导出都在浏览器里做得好）；**桌面窗口 = 原生交互**（实时字幕与纠正编辑器、区域/窗口框选、
  暂停与全局热键、目标语言），并用「打开设置面板」一键把用户交给面板。此前桌面上的
  设置/呈现/诊断/翻译四个标签是同一份状态的第二个编辑器，已删除。
- 纠正必须**赢得过已经在跑的异步精修**：两层显示意味着模型往往正在精修同一行，几百毫秒后
  才返回。因此词库变更会推进一个 revision，早于该变更排队/缓存的模型结果一律丢弃并计数
  （`refinements_stale`）——否则"纠正是否生效"取决于哪个线程先结束。同理，手工改词条文件
  也不会再被模型更早的答案盖掉。
- 实测状态：原型中的 `correct` / `list_corrections` / `remove_correction` 命令、
  桌面端「字幕」页的纠正编辑器、Web 面板的只读列表；`selfcheck_correct` 覆盖
  文件格式、松匹配、两种粒度、复用缓存失效与画面重绘。

### R3 支持自定义规则

- 当待译词汇**不在语料库中**时，通过规则尝试推演其翻译。
- 规则类型：

| 类型 | 说明 | 示例 |
| --- | --- | --- |
| 词根词缀拆分 | 拆分前缀/词根/后缀后组合语义 | `over-` + 已知词 |
| 构词模板 | 按作品内命名法套用模板 | 「XX 境」→ `XX Realm` |
| 音译转写 | 按目标语言音系转写 | 自造专有名词 |
| 领域映射 | 按作品/题材映射到既定译法 | 修仙术语表 |
| 组合还原 | 已知词素拼接成新词 | `void` + `blade` |

- **可解释性**：每条规则命中必须返回所命中的规则标识与置信度，便于人工校对。
- **优先级**：语料库命中优先于规则推演；规则推演结果必须可被用户一键写入语料库（自学习闭环）。
  原型已实现并验证：规则以 0.55 的置信度给出 `antidragon → 反龙`，接受这条纠正后同一个词改由
  语料库以 1.0 的置信度给出、译文不变，来源从 `rule:affix` 变为 `corpus:`。
- **规则语言过滤**：规则的 `target` 按语言比较（写 `zh`、`zh-CN`、`zho_Hans` 都算中文）。
  早期实现是字符串相等，于是把目标写成 `zh` 就会让全部中文规则静默失效、看起来像规则引擎坏了。
- 规则定义与语料库同为**外置文件**，用户编辑规则无需修改主引擎、无需重新编译。

### R4 低延迟优先

实时识别场景下，端到端延迟必须有明确预算并被实测标定：

| 阶段 | 目标延迟 |
| --- | --- |
| 热键响应 | < 50 ms |
| 屏幕捕获 + 图像预处理 | < 30 ms |
| OCR（单帧，中小区域） | < 150 ms |
| 语料库 / 规则匹配 | < 10 ms |
| 本地 LLM 辅助（仅未命中时触发） | < 800 ms，支持流式返回 |
| 悬浮窗渲染 | < 16 ms（60 FPS） |

延迟优化手段：

- OCR 与 LLM 推理全部移出 UI 线程，使用工作线程池
- 帧缓冲双缓冲，避免整帧频繁拷贝
- 识别前裁剪无关区域、必要时降采样
- **增量识别**：仅识别发生变化的区域
- 结果缓存：重复文本模式直接命中缓存
- LLM 按需触发：仅在语料库与规则均未命中时调用

---

# **功能扩展**

## 高兼容性

支持电脑端（Linux / Windows / macOS）、移动端（Android）以及其他小型设备。

| 平台 | 支持级别 | 关键依赖 |
| --- | --- | --- |
| Windows | 一级（优先落地） | 桌面截屏 API、全局热键 |
| Linux | 一级 | X11 / Wayland 截屏、XTest |
| macOS | 一级 | ScreenCaptureKit / CGWindowList |
| Android | 二级 | MediaProjection 截屏、悬浮窗与无障碍权限 |
| 其他小型设备 | 三级 | 需裁剪 UI 与模型体积，仅保留核心链路 |

实现策略：

- **核心引擎与平台层解耦**：OCR 调度、语料库、规则、翻译路由构成平台无关内核。
- 平台差异封装在**捕获层**与 **UI 适配层**，新增平台只需实现这两个适配接口。
- 移动端复用同一套语料库与规则文件格式，保证跨端译法一致。

## 可扩展性

支持用户自定义插件，用于扩展本体程序的能力。

- **可挂载的扩展点**：

| 扩展点 | 用途 | 当前状态（`prototype/`） |
| --- | --- | --- |
| 翻译后处理 | 术语统一、风格改写 | **已接通**，按加载顺序链式处理 |
| 导出格式 | 导出字幕、术语表、对照稿 | **已接通**，`export` 命令与 `--export` |
| 语料库加载器 | 接入自有格式词库 | 已预留（`corpus_loader`） |
| 规则集 | 注入自定义推演规则 | 已预留 |
| OCR 前/后处理 | 特殊字体、竖排、批注处理 | 已预留 |
| 悬浮窗渲染 | 自定义对照排版与主题 | 已预留（声明式规格已覆盖大部分需求） |

- **插件形式**：本地动态库（C++）或脚本插件（Python），分别置于 `plugins/builtin/`（内置）与 `plugins/user/`（用户）。
- **稳定性要求**：插件接口需版本化并保持向后兼容；单个插件崩溃或超时不得影响主程序，需具备隔离与超时保护。
  - 原型现状：`PLUGIN_API_VERSION = 1`，版本不匹配、抛异常、返回类型错误、声明未知扩展点四类失败
    全部被捕获、记录并跳过，不中断主程序（35 项自检覆盖）。
- **安全要求**：插件加载需校验来源，默认限制其网络与文件访问权限。
  - **原型做不到这一点，且不应假装做到**：Python 插件运行在同一进程内，无法真正沙箱化
    （无权限降级、无网络拦截）。因此当前定位是「给自己机器用的可信插件」，
    **不适合分发给他人**；受限执行需要子进程 + 权限隔离，属于 C++ 主干的工作。

---

# **核心功能**

- **即时屏幕识别翻译**：无需复制粘贴，悬浮窗识别屏幕文本，实现一键双语对照翻译。
- **虚构词汇自学习与翻译**：针对科幻、魔幻作品，用户可设定翻译原则，系统自动对原本不存在的词语进行语义推理和自动化加工。
- **网络用语语义库**：维护并更新网络热梗语料，提供符合当代语境的翻译。
- **本地化计算**：基于本地算法进行翻译处理，减少对外部 API 的依赖。

---

# **技术架构**

整体分层：**UI 层 / 服务层 / 核心翻译层 / 数据与模型层**

主链路：

```
热键选区 → 屏幕捕获 → 图像预处理 → OCR → 文本规范化
        → 语料库匹配 → 规则推演 → （未命中）本地 LLM
        → 结果缓存 → 悬浮窗双语渲染
```

设计原则：

1. **本地优先**：主链路所有组件默认在本机运行，网络仅用于可选的插件能力。
2. **规则与语料库外置**：扩展翻译能力无需修改主引擎。
3. **可解释**：每次翻译均可追溯命中来源（语料库 / 规则 / LLM）及置信度。
4. **线程隔离**：OCR 与 LLM 推理不占用 UI 线程，保证低延迟与界面流畅。
5. **平台解耦**：平台差异仅存在于捕获层与 UI 适配层。

语言选型：核心引擎与平台层采用 **C/C++**（构建脚本为 `CMakeLists.txt`），脚本插件与实验性模块可采用 **Python**。

---

# 项目结构

以下为仓库实际结构与目标结构对照，标注 *待实现* 的为规划中内容：

```
ProjectWatashi/
├── CMakeLists.txt            # C++ 工程构建脚本
├── README.md                 # 项目说明
├── Project Watashi.md        # 项目目标与需求文档（本文件）
├── prototype/                # Python 验证原型（当前唯一可运行实现）
│   ├── watashi_proto.py      # CLI 入口（Session 的薄客户端）
│   ├── watashi/session.py    # 引擎门面：事件出、命令入
│   ├── watashi/events.py     # 契约：事件/命令 schema，零 UI 依赖
│   ├── watashi/overlay.py    # 悬浮窗
│   ├── watashi/desktop.py    # 桌面窗口（与悬浮窗共用 Tk root）
│   ├── watashi/plugins.py    # 插件扩展点与失败隔离
│   ├── watashi/capture.py    # 区域捕获 / 窗口捕获 / 变化检测
│   ├── watashi/local_nmt.py  # 本地模型、术语保护
│   ├── plugins/              # 实际插件目录（builtin / user）
│   └── selfcheck_*.py        # 8 个自检脚本
├── config/                   # 系统配置与用户偏好（app.yaml）
├── docs/                     # 项目开发文档
│   ├── architecture.md       # 架构总览
│   ├── screen_ocr_design.md  # 屏幕 OCR 模块设计
│   └── plugin-api.md         # 插件 API 草案
├── include/                  # 公共头文件
│   ├── app/                  # 应用装配
│   ├── core/                 # 翻译引擎接口
│   ├── db/                   # SQLite 数据访问接口
│   ├── llm/                  # 本地 LLM 后端接口
│   ├── ocr/                  # 屏幕捕获与 OCR 接口
│   └── plugins/              # 插件接口（IPlugin）
├── src/
│   ├── app/                  # 程序入口与装配
│   ├── core/                 # 翻译路由与分发
│   │   ├── translator_engine.cpp
│   │   ├── fiction_engine/   # *待实现* 虚构词汇推演算法
│   │   └── network_slang/    # *待实现* 网络用语处理
│   ├── ocr/                  # 屏幕捕获、预处理、OCR 调度
│   ├── db/                   # *待实现* 本地 SQLite 词库与缓存
│   ├── llm/                  # *待实现* 本地 LLM 后端
│   ├── rules/                # *待实现* 规则引擎
│   ├── ui/                   # *待实现* 悬浮窗与主控制台
│   ├── plugins/              # *待实现* 插件加载与隔离
│   └── utils/                # *待实现* 日志、字符串、加解密
├── rules/                    # 可编辑语料库与规则（R2 / R3）
│   ├── slang/                # 网络用语库
│   ├── fiction/              # 虚构词汇库
│   └── dictionaries/         # 通用词典
├── models/                   # 本地模型（ocr / llm / embedding），已 gitignore
├── plugins/                  # 插件目录（builtin / user）
└── tests/                    # C++ 测试（目前仅 CMakeLists.txt，无测试源文件）
```

> 注：早期草案中的 `bin/`、`dictionary/` 已分别并入构建产物与 `rules/`，`src/database/` 统一为 `src/db/`。
>
> 注：标注 *待实现* 的 C++ 目录目前均为占位骨架；已落地的功能在 `prototype/`，
> 两者共用同一套契约（事件/命令 schema）与同一份插件接口草案（`docs/plugin-api.md`）。
>
> 注：原脚手架中的空目录 `ui/`、`scripts/`，以及早期的 Python 桥接层 `src/bridge/`
> （连同其加载的 `plugins/fiction_plugin.py`、`plugins/slang_plugin.py`）已删除：
> 它们实现的是早期接口 `translate(text, src, tgt) -> dict`，与现行插件注册表
> （`postprocess` / `export`）不兼容，且全仓库无任何引用。
> `CMakeLists.txt` 中长期存在 `add_subdirectory(tests)` 而 `tests/CMakeLists.txt` 缺失的问题，
> 会导致 **CMake 配置期直接失败**，现已补上该文件。

---

# **安全与合规**

- 引入智能代码辅助工具进行代码编写，并结合人工安全检测，确保程序无漏洞、无后门，安全嵌入无冲突。
- 全链路本地处理：用户文本、术语库与翻译记录**不上传**。
- 插件需校验来源与完整性，默认最小权限（限制文件与网络访问）。

---

# **里程碑（建议）**

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| M1 最小闭环 | 热键选区 → 截图 → OCR → 语料库匹配 → 悬浮窗双语显示（Windows） | **已完成**（`prototype/`） |
| M2 可定制 | 语料库与规则引擎落地，支持用户自定义与热加载（R2 / R3） | **已完成**（`prototype/`，含实时纠正闭环） |
| M3 本地 LLM | 未命中词触发本地模型辅助翻译，含流式输出与缓存（R1） | **已完成**（本地 NMT，非流式，异步精修） |
| M4 低延迟达标 | 增量识别、缓存、线程模型优化，实测达成延迟预算（R4） | 部分完成（实测见下） |
| M5 跨平台 | Linux / macOS 捕获层与 UI 适配 | 未开始 |
| M6 插件化与移动端 | 插件 API 稳定化，Android 端实验验证 | 插件骨架**已完成**（`prototype/watashi/plugins.py`，API v1，示例插件 2 个，35 项自检通过）；Android 端未开始 |

> 以上 M1–M3 的验证实现为 Python 原型（`prototype/`），用于先测准管线与延迟数据；
> C++ 主干（`src/`）仍为占位实现，尚未承接这些成果。

## 界面架构

采用「**一个引擎，多个界面**」：引擎通过统一的**事件/命令契约**对外暴露，
每个界面只是该契约的适配器。

```
                    ┌─────────────────────────────────────┐
                    │  Session（引擎，不引用任何 UI 工具包） │
                    └──────────────┬──────────────────────┘
                                   │
                  事件出 ───────────┴────────── 命令入
                  ready/subtitle/              pause, resume, set_region,
                  refinement/stats/            use_window, set_target_lang,
                  status/presentation/         set_fps, reload_corpus,
                  error/stopped                set_presentation, load_profile
                                   │
        ┌──────────────┬───────────┴────────┬──────────────────┐
        │              │                    │                  │
   CLI / PowerShell   悬浮窗             桌面窗口          本地面板
   --json-lines     进程内队列        共用同一 Tk root     SSE + POST
```

五条设计性质：

1. **界面是适配器，不是第二份程序**——新增界面约 100 行。
2. **契约模块零依赖**（无 tkinter、无 Web 框架、无模型），引擎可在没有 UI 工具包的机器上运行。
3. **信封带版本与序号**（`v` / `seq` / `ts`），客户端能识别不兼容的流，而不是默默读错字幕。
4. **捕获源可注入**，因此除悬浮窗自身外全链路可无头验证。
5. **捕获源可在运行中切换**（`use_window` / `set_region`），因为两种模式互斥：
   窗口捕获跟随窗口移动，固定矩形则不会。

界面分工：

| 界面 | 定位 |
| --- | --- |
| CLI / PowerShell | 自动化、CI、无头与远程；`--json-lines` 提供机器可读流；**全部功能操作的入口** |
| 悬浮窗 | 原生、点击穿透、约 6 MiB；**保持最小、不做深度定制** |
| 桌面窗口 | `--desktop`：控制、设置与实时输出（字幕/采集/翻译/呈现/插件/诊断六个页签）；与悬浮窗**共用同一个 Tk root**，因为两个 `Tk()` 实例的控件无法互相放置 |
| Web 面板 | **仅用于调整设置与阅览**：外观版式编辑、引擎设置、字幕与统计只读查看；与引擎同进程，默认仅监听 loopback；**不实现任何功能操作** |
| 局域网 | 同一契约的另一传输，**默认关闭**，需显式开启 |

> Web 面板的功能边界在**服务端强制**：`/api/command` 只接受设置类命令，
> 其余（如 `pause`、`reload_corpus`、`shutdown`）一律返回 403 并说明应改用 CLI。
> 只在界面上删按钮是不够的——直接 POST 同样要拦住。

## 客制化（声明式呈现规格）

四类客制化共用一份**声明式呈现规格**，由所有界面共同遵守
（完整 schema 见 `docs/presentation-spec.md`）：

| 类别 | 内容 |
| --- | --- |
| A 外观 | 字体、字号、颜色、透明度、描边、色块或透明背景 |
| B 版式 | 锚点、偏移、对齐、逐行分块、**译文覆盖原文位置（原位覆盖）** |
| C 行为 | 语料库、规则、翻译后端 |
| D 配置档 | 按作品/项目打包的命名配置，一键切换 |

内置预设：`bar` / `bare` / `minimal` / `lines` / `inplace` / `panel` / `hidden`。

**为什么用声明式而不是代码渲染器**：代码渲染器意味着把用户代码载入引擎进程，
而 Python 无法真正沙箱化，与「单插件崩溃不得影响主程序」「插件最小权限」两条要求冲突。
声明式规格既避开该问题，又带来更大的收益：**一份描述，所有界面都遵守**。

**前置改动（已完成）**：字幕事件新增 `lines` 数组，逐行携带 `box`（区域相对坐标）。
没有坐标，「原位覆盖」与逐行版式根本无法表达；而该字段必须在有外部客户端之前加入，
否则就是破坏性变更。

---

## 验证进展（`prototype/`）

已用真实屏幕实测，全部本地运行、无云端调用：

| 项目 | 实测 |
| --- | --- |
| 端到端延迟（区域 1200×200） | OCR 70–104 ms，语料库翻译 0.1–0.5 ms |
| 变化检测 | 约 94% 的帧因画面未变而跳过 OCR |
| 本地翻译模型 | NLLB-200-distilled-600M int8，617 MiB，加载约 1.3 s |
| 模型延迟 | 单句 208–250 ms；批量 6 句时 71–86 ms/句（快 3.5 倍） |
| 术语保护 | 12 个语料库术语成功穿过模型，0 改写 |
| 模型与 OCR 争用 | 并发时 OCR 由 73 ms 恶化至 266 ms，已通过让模型的空闲期调度规避 |
| 自检覆盖 | 8 个自检脚本，298 项断言全部通过（其中 5 个需要显示器） |
| 内存占用 | 仅 OCR 175 MiB；OCR + 模型 + 悬浮窗 约 890 MiB（峰值 1123 MiB） |

**核心结论**：通用 NMT 模型会把虚构术语译错且每次不一致（`voidsword` → “无效的字符”），
因此**语料库必须保持权威，模型只负责补足语法**。这一混合链路已实现并测通，
也正是本项目区别于通用翻译工具的地方。

